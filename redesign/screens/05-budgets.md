# Budgets — inline on Home (BudgetModal deleted)

- Home shows top 3 budget rows; "Editar" toggles edit mode in place (rows stay put, no modal).
- Edit mode: each limit becomes a stepper — [−] R$ 600 [+] (R$ 50 increments, long-press accelerates) — plus tap-to-type. Caption above list: "Toque em um valor para ajustar". Done = "Concluir" ghost in section header.
- "Ver tudo" → full Budgets screen: same rows for every category + monthly target row at top + "Nova categoria" ghost at end.
- Over-budget rows: bar + amount in danger; caption "R$ {x} acima".
- No targets/goals gamification, no emoji, no confetti.
